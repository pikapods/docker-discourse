DISCOURSE_IMAGE=container-discourse:local
# Generate once with `openssl rand -hex 64`; keep stable across recreates.
# Rotating this value invalidates sessions and corrupts encrypted columns.
DISCOURSE_SECRET_KEY_BASE=
